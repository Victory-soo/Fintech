const car = {
    name: "Bentley",
    ph: 180,
    maker: "hyundai",
};

// let name = car.name;
// let ph = car.ph;

// console.log(name, ph);

const { name, ph, maker } = car;
console.log(name, ph, maker);
